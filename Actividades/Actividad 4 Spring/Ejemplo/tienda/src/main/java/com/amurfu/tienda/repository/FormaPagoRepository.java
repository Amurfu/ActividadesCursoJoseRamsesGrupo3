package com.amurfu.tienda.repository;

import com.amurfu.tienda.data.FormasPago;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FormaPagoRepository extends JpaRepository<FormasPago,Integer> {
}
